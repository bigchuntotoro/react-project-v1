<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.board.mapper.BoardMapper">

    <!-- 동적 검색 조건 공통 SQL -->
    <sql id="search">
        <if test="keyword != null and keyword != ''">
            <choose>
                <when test="searchType == 'title'">
                    AND b.title LIKE CONCAT('%', #{keyword}, '%')
                </when>
                <when test="searchType == 'content'">
                    AND b.content LIKE CONCAT('%', #{keyword}, '%')
                </when>
                <when test="searchType == 'writer'">
                    AND b.writer LIKE CONCAT('%', #{keyword}, '%')
                </when>
                <otherwise>
                    AND (b.title LIKE CONCAT('%', #{keyword}, '%')
                    OR b.content LIKE CONCAT('%', #{keyword}, '%'))
                </otherwise>
            </choose>
        </if>
    </sql>

    <!-- 1. 게시글 목록 조회 (페이징 + 검색 + 파일 개수) -->
    <select id="selectBoardList" parameterType="com.example.board.dto.SearchDto" resultType="com.example.board.dto.BoardDto">
        SELECT
        b.board_id AS boardId,
        b.title,
        b.writer,
        b.read_cnt AS readCnt,  <!-- 👈 DTO 필드명 readCnt 와 일치 -->
        b.created_at AS createdAt,
        COUNT(f.file_id) AS fileCount
        FROM reactboard b
        LEFT JOIN reactboard_file f ON b.board_id = f.board_id
        WHERE 1=1
        <include refid="search"/>
        GROUP BY b.board_id, b.title, b.writer, b.read_cnt, b.created_at
        ORDER BY b.board_id DESC
        LIMIT #{offset}, #{recordSize}
    </select>

    <!-- 2. 게시글 총 개수 (페이징용) -->
    <select id="selectBoardCount" parameterType="com.example.board.dto.SearchDto" resultType="int">
        SELECT COUNT(*)
        FROM reactboard b
        WHERE 1=1
        <include refid="search"/>
    </select>

    <!-- 3. 게시글 상세 조회 -->
    <select id="selectBoardById" parameterType="long" resultType="com.example.board.dto.BoardDto">
        SELECT
        b.board_id AS boardId,
        b.title,
        b.content,
        b.writer,
        b.read_cnt AS readCnt,  <!-- 👈 DTO 필드명 readCnt 와 일치 -->
        b.created_at AS createdAt,
        (SELECT COUNT(*) FROM reactboard_file WHERE board_id = b.board_id) AS fileCount
        FROM reactboard b
        WHERE b.board_id = #{boardId}
    </select>

    <!-- 4. 특정 게시글의 첨부파일 목록 조회 -->
    <select id="selectFilesByBoardId" parameterType="long" resultType="com.example.board.dto.BoardFileDto">
        SELECT
        file_id AS fileId,
        board_id AS boardId,
        original_name AS originalName,
        save_name AS saveName,
        file_size AS fileSize,
        created_at AS createdAt
        FROM reactboard_file
        WHERE board_id = #{boardId}
        ORDER BY file_id ASC
    </select>

    <!-- 5. 단일 첨부파일 정보 조회 (다운로드용) -->
    <select id="selectFileById" parameterType="long" resultType="com.example.board.dto.BoardFileDto">
        SELECT
        file_id AS fileId,
        board_id AS boardId,
        original_name AS originalName,
        save_name AS saveName,
        file_size AS fileSize,
        created_at AS createdAt
        FROM reactboard_file
        WHERE file_id = #{fileId}
    </select>

    <!-- 6. 게시글 등록 (Auto Increment PK 반환) -->
    <insert id="insertBoard" parameterType="com.example.board.dto.BoardDto" useGeneratedKeys="true" keyProperty="boardId">
        INSERT INTO reactboard (title, content, writer, read_cnt, created_at)
        VALUES (#{title}, #{content}, #{writer}, 0, NOW())
    </insert>

    <!-- 7. 파일 정보 다중 등록 (Bulk Insert) -->
    <insert id="insertBoardFiles" parameterType="list">
        INSERT INTO reactboard_file (board_id, original_name, save_name, file_size, created_at)
        VALUES
        <foreach collection="list" item="file" separator=",">
            (#{file.boardId}, #{file.originalName}, #{file.saveName}, #{file.fileSize}, NOW())
        </foreach>
    </insert>

    <!-- 8. 게시글 수정 -->
    <update id="updateBoard" parameterType="com.example.board.dto.BoardDto">
        UPDATE reactboard
        SET title = #{title},
        content = #{content}
        WHERE board_id = #{boardId}
    </update>

    <!-- 9. 조회수 1 증가 -->
    <update id="updateReadCount" parameterType="long">
        UPDATE reactboard
        SET read_cnt = read_cnt + 1
        WHERE board_id = #{boardId}
    </update>

    <!-- 10. 게시글 삭제 -->
    <delete id="deleteBoard" parameterType="long">
        DELETE FROM reactboard
        WHERE board_id = #{boardId}
    </delete>

    <!-- 11. 특정 파일 ID 목록 삭제 -->
    <delete id="deleteFilesByIds" parameterType="list">
        DELETE FROM reactboard_file
        WHERE file_id IN
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>